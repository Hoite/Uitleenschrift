# Voorbeeld configuratie bestand voor Uitleenschrift
# Kopieer dit bestand naar .env en pas de waarden aan

# Flask geheime sleutel - genereer een nieuwe voor productie gebruik
# Genereer een sterke sleutel met: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=verander-dit-naar-een-sterke-geheime-sleutel-voor-productie

# Database URL (standaard SQLite in instance directory)
DATABASE_URL=sqlite:///instance/uitleenschrift.db

# Debug modus (zet op False voor productie)
DEBUG=False

# Google Books API (optioneel, voor automatische covers)
# Verkrijg een API key via: https://console.developers.google.com/
GOOGLE_BOOKS_API_KEY=

# Cloudflare Tunnel (optioneel, voor eenvoudige tunnel setup)
CLOUDFLARE_TUNNEL_TOKEN=
